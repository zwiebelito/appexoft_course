/*
function celciusToFahrenheit(celsius: number): number {
    return (celsius*9/5)+32;
}

const celsiusTemperature: number = 30;
const fahrenheit = celciusToFahrenheit(celsiusTemperature);
console.log(`${celsiusTemperature} C is equal to ${fahrenheit}F`)*/


/*
enum DaysOfWeek {
    Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday
}

function isWorkDay(day: DaysOfWeek):boolean {
    return day >= DaysOfWeek.Monday && day <= DaysOfWeek.Friday
}

const today = DaysOfWeek.Sunday;
console.log(`Is today a working day? ${isWorkDay(today) ? 'yes': 'no'}`)*/

